import React from 'react';

const NextSection: React.FC = () => {
    return (
        <section id="next-section" className="w-full min-h-screen bg-gray-100 flex items-center justify-center">
            <h2 className="text-3xl font-bold">Welcome to the Next Section!</h2>
        </section>
    );
};

export default NextSection;
